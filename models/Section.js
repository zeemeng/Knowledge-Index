export default class Section {
  /**
   * @param {number} id
   * @param {string} name
   * @param {string} longName
   */
  constructor(id, name, longName) {
    this.id = id;
    this.name = name;
    this.longName = longName;
  }
}
